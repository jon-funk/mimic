{
  "name": "mimic",
  "version": "0.0.1",
  "description": "A restful wrapper for nektos/act to provide a GitHub runner-like experience on-prem without a full GitHub runner provisioned",
  "main": "src/server.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Funk",
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
